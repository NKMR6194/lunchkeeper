= render "partials/shopheadbar"
%h1 Login
= render 'form'
:javascript
  $(function() {
  <haml_silent>	 if flash[:notice]
  </haml_silent><haml_block>	    alert("#{flash[:notice]}");
  </haml_block>});
